// Intentionally empty module.
//
// We keep large docstrings close to the crate root (`lib.rs`) where pyo3 attaches them to the
// module, but we want the actual implementation split across smaller files.

